name: Clean terraform branch environment

on:
  pull_request:
    # Trigger cleanup when a PR lifecycle ends.
    types: [closed]
env:
  TERRAFORM_DIR: Terraform/AmplifyBranches
  
jobs:
  prepare:
    name: Prepare Metadata
    runs-on: ubuntu-latest
    outputs:
      branch_name: ${{ steps.meta.outputs.branch_name }}
      workspace: ${{ steps.meta.outputs.workspace }}
    steps:
      - name: Compute workspace, image tag and build config
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          RAW_REF="${GITHUB_HEAD_REF:-${GITHUB_REF_NAME}}"
          WORKSPACE="$(echo "$RAW_REF" | grep -oE '[A-Za-z]+-[0-9]+' | head -n1 | tr '[:upper:]' '[:lower:]' || true)"
          if [[ -z "$WORKSPACE" ]]; then
            WORKSPACE="$(echo "$RAW_REF" \
              | tr '[:upper:]' '[:lower:]' \
              | sed -E 's#[^a-z0-9-]#-#g' \
              | sed -E 's#-+#-#g' \
              | sed -E 's#(^-|-$)##g' \
              | cut -c1-18)"
          fi
          if [[ -z "$WORKSPACE" ]]; then WORKSPACE="default"; fi
          WORKSPACE="${WORKSPACE}"    
          echo "branch_name=$RAW_REF" >> "$GITHUB_OUTPUT"
          echo "workspace=$WORKSPACE" >> "$GITHUB_OUTPUT"
  terraform_destroy:
    name: Terraform destroy (Branch environment)
    runs-on: ubuntu-latest
    needs: prepare
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        
      - name: Select/Create workspace
        working-directory: ${{ env.TERRAFORM_DIR }}
        run: terraform workspace select -or-create "${{ needs.prepare.outputs.workspace }}"
          
      - name: Terraform destroy
        working-directory: ${{ env.TERRAFORM_DIR }}
        env:
          TF_VAR_branch_name: ${{ github.head_ref || github.ref_name }}
        run: terraform destroy -auto-approve
        
      - name: Delete terraform workspace after destroy
        working-directory: ${{ env.TERRAFORM_DIR }}
        shell: bash
        run: |
          set -euo pipefail
          terraform workspace select default
          terraform workspace delete "${{ steps.workspace.outputs.workspace }}" || true