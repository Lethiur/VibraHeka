/**
 * Represents the data required for a user to log in.
 *
 * This interface is used to encapsulate the essential credentials
 * needed for authentication purposes.
 */
export interface LoginData {
    email: string;
    password: string;
}